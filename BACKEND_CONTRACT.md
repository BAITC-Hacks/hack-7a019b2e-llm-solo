# Контракт фронтенда TRACE для Muha

Команда: пользователь + Adilet — frontend, Muha — backend.
Этот файл описывает существующий фронтенд в ветке Zhans. Он не реализует сервер и не предполагает, что API уже готов.

## Как подключить результат анализа

Сейчас frontend/app.js, функция loadDefault(), читает ../out/graph.json.
Есть два варианта интеграции:

1. Бэкенд сохраняет полный результат в out/graph.json. Из корня репозитория запускается локальный HTTP-сервер, интерфейс открывается по /frontend/.
2. Бэкенд отдаёт тот же объект через API. Согласуйте URL и замените адрес fetch в loadDefault(). Лучше раздавать API и frontend с одного origin. Если порты разные, разрешите CORS только для нужного адреса фронтенда.

Кнопка «Импорт данных» также читает локальный JSON без отправки на сервер.
Датасет и рассчитанный out/graph.json не входят в этот frontend-коммит: папки data/ и out/ исключены через .gitignore.

## Формат ответа

Объект содержит непустой массив nodes и массив edges. Ниже — синтетический пример формата, не результат анализа датасета.

```json
{
  "nodes": [
    {
      "gid": "100000000000000001",
      "role": "distributor",
      "role_score": 0.75,
      "cluster_id": 0,
      "priority_score": 0.65,
      "evidence": "Пример: исходящий оборот 90000 KZT, 3 перевода на 1 контрагента.",
      "depth": 0,
      "is_seed": true,
      "in_kzt": 0,
      "out_kzt": 90000,
      "truncated_by_depth": false
    },
    {
      "gid": "100000000000000002",
      "role": "terminal",
      "role_score": 0.7,
      "cluster_id": 0,
      "priority_score": 0.3,
      "evidence": "Пример: входящий оборот 90000 KZT, исходящих переводов в выборке нет.",
      "depth": 1,
      "is_seed": false,
      "in_kzt": 90000,
      "out_kzt": 0,
      "truncated_by_depth": false
    }
  ],
  "edges": [
    {
      "src": "100000000000000001",
      "dst": "100000000000000002",
      "sum_kzt": 90000,
      "n_tx": 3
    }
  ]
}
```

Пример иллюстрирует поля, а не критерии присвоения ролей.

## Значение полей и ограничения

| Поле | Требование |
| --- | --- |
| gid, src, dst | Строки десятичных цифр. Не JavaScript Number: длинные int64 округляются. |
| role | coordinator, consolidator, distributor, transit, terminal или peripheral. |
| role_score, priority_score | Конечные числа от 0 до 1. Интерфейс показывает шкалу 0–100. |
| cluster_id | Целочисленный идентификатор группы: число или строка. |
| evidence | Текстовое численное обоснование роли/приоритета. HTML не нужен. |
| depth | Неотрицательное целое: минимальная глубина от seed, seed имеет глубину 0. |
| is_seed | Boolean true/false. |
| in_kzt, out_kzt | Неотрицательные численные обороты в тенге внутри наблюдаемого графа. |
| truncated_by_depth | Boolean: есть ограничение видимости на границе обхода. |
| sum_kzt | Неотрицательная суммарная величина переводов по направленной паре. |
| n_tx | Неотрицательное целое количество переводов этой пары. |

Передавайте все перечисленные поля. Для некоторых отсутствующих численных полей текущий frontend использует 0, но это совместимость загрузчика, а не замена расчёту метрик.

- Каждый gid уникален; обе вершины каждого ребра присутствуют в nodes.
- Передавайте всю сеть, не только top-20. Ограничение числа видимых узлов делает frontend.
- Одна запись edges на направленную пару src → dst; обратное направление — отдельная запись.
- in_deg и out_deg frontend вычисляет по уникальным контрагентам. Роли и скоринг он не рассчитывает.
- На depth >= 4 без исходящих frontend дополнительно отмечает границу данных. Не делайте из отсутствия исходящих автоматический вывод о конечном получателе.
- Для seed-клиентов интерфейс предупреждает, что входящие потоки могут быть неполными.
- Не передавайте NaN/Infinity, отрицательные суммы и ссылки на неизвестные gid.

## Что использует интерфейс

- Обзор и граф: nodes + edges; точный поиск любого gid показывает всех прямых соседей.
- Очередь проверки: priority_score; экспорт CSV использует evidence как why.
- Кластеры: cluster_id; внутренний оборот считается только по рёбрам с обоими концами в группе.
- Карточка клиента: роль, скоры, evidence, обороты, все входящие/исходящие связи.
- Самый приоритетный клиент текущей выборки попадает в заметку «Начальная точка».

CSV-файлы сдачи (nodes_roles.csv, clusters.csv, top_nodes.csv) — отдельные результаты пайплайна. Браузер не читает их вместо graph.json.

## Проверка совместимости

Из корня репозитория:

```sh
node --test frontend/data.test.cjs
python -m http.server 8000 --bind 127.0.0.1
```

Откройте http://127.0.0.1:8000/frontend/ и импортируйте результат или положите его в out/graph.json. Проверьте точный gid, карточку, окружение, фильтры и экспорт.

Важно: загрузку графа сохраняйте через response.arrayBuffer() → decodeGraph(): внутри байты хешируются для AI, а TextDecoder → MoneyGraph.parse() → MoneyGraph.normalize() сохраняют точные GID. Не заменяйте на response.json(): текущий пайплайн может выдавать int64 без кавычек. Новый API должен сразу возвращать идентификаторы строками.

Дизайн и правила расширения UI: frontend/DESIGN.md. Описание фронтенда: frontend/README.md.

## ИИ-помощник

Дополнение к контракту графа: [AI_CONTRACT.md](AI_CONTRACT.md) — `POST /api/ai/analyze`, схема ответа, память, повторы, SHA-256 выгрузки и подключение OpenAI. Серверную часть реализует Muha. Ключи в браузер не передавать.
